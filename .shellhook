#!/usr/bin/env bash

set -e

expression=$(grep "export" < nixGL/result/bin/nixGL*)
if [ -n "$expression" ]; then
    eval "$expression"
fi

if [ ! -d "$WD/bin" ]; then
    mkdir "$WD/bin"
fi

export LD_LIBRARY_PATH="$WD/glfw/src:$LD_LIBRARY_PATH"

set +e
